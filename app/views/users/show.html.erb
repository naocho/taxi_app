<div class="contents row">
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @taxis.each do |taxi| %>
    <div class="content_post" style="background-image: url(<%= taxi.image %>);">
      <p><%= taxi.text %></p>
      <span class="name"><%= taxi.name %></span>
    </div>
  <% end %>
</div>